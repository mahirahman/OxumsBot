module.exports = {
    name: 'setupdates',
    description: '!setupdates command will link the bot to a discord channel where Rust updates will be posted.',
    execute(message, args) {

        return message.channel.send('Placeholder');
    },
};