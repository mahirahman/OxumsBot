module.exports = {
    name: 'map',
    description: '!map command will reply with an image of the server map.',
    execute(message, args) {

        return message.channel.send('Placeholder');
    },
};