module.exports = {
    name: 'server',
    description: '!server command will reply with common server information of the Rust server',
    execute(message, args) {

        return message.channel.send('Placeholder');
    },
};